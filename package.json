{
  "name": "@f/to-inline-style",
  "description": "Convert style object to plain strings for inline html styles",
  "repository": "git://github.com/micro-js/to-inline-style.git",
  "version": "0.1.3",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/css-default-units": "^1.0.1",
    "@f/hyphenate": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
